# ADRs no longer live here

As of 2026-08-15 (PR-D0), all Architecture Decision Records live at
[`/docs/adr/`](../../adr/) at the repo root, alongside the RFCs
(`/docs/rfcs/`), project status notes (`/docs/project_status/`), and
code reviews (`/docs/review/`).

This directory is intentionally empty. Do not create new ADRs here.

For the rationale and migration history, see the commit that introduced
this directory: `step-pr-d0-rename-adrs`.
